import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const Container = styled.div`
   ${tw``}
`

const SelectDate = styled.input`
   ${tw`border border-gray-400 border-solid `}
   width:100%;
   height: 40px;
`

export { Container, SelectDate }
